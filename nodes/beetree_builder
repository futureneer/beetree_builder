#!/usr/bin/env python

import sys

from rqt_gui.main import Main
from beetree_builder.builder import Builder

plugin = 'beetree_builder'
main = Main(filename=plugin)
sys.exit(main.main(standalone=plugin))#, plugin_argument_provider=Dot.add_arguments))
